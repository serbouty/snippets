/* Include the header file only once when compiling. */
#pragma once

/* Make an arithmetic operation from user input. */
class Calculator
{
	/* Simple declaration, implementation available in Calculator C++ file. */
public:
	double Calculate(double x, char operation, double y) const;
};